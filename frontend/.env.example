# Frontend environment
# Leave VITE_API_BASE empty in development so Vite's /api proxy is used.
# In production set it to the public API origin, e.g. https://portal.ukzn.ac.za
VITE_API_BASE=

# Dev-server proxy target (development only)
VITE_API_PROXY_TARGET=http://127.0.0.1:3001
